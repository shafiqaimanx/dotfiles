[module/cpu]
type = internal/cpu

interval = 2
format-prefix = "%{T2} %{T-}"
format-prefix-foreground = ${colors.sky}
label = %percentage:2%%

warn-percentage = 80
warn-percentage-foreground = ${colors.red}
